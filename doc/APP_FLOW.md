# 应用流程文档 (APP_FLOW.md)
## 亏否 - 资产管理应用流程图

---

## 1. 应用整体架构

```
亏否应用
├── 首页（资产列表）
├── 添加页面
└── 个人中心
```

---

## 2. 页面流程图

### 2.1 应用启动流程

```mermaid
graph TD
    A[启动应用] --> B{首次使用?}
    B -->|是| C[显示引导页面]
    B -->|否| D[加载用户数据]
    C --> E[创建用户配置]
    E --> F[进入首页]
    D --> F
    F --> G{有资产数据?}
    G -->|是| H[显示资产列表]
    G -->|否| I[显示空状态引导]
    I --> J[提示添加第一件物品]
```

### 2.2 首页交互流程

```mermaid
graph TD
    A[首页] --> B[资产总览卡片]
    A --> C[筛选排序区]
    A --> D[资产列表]
    A --> E[底部导航]
    
    B --> B1[显示总残值]
    B --> B2[显示今日成本]
    B --> B3[显示资产数量]
    B --> B4[显示预计每日成本]
    
    C --> C1[搜索名称]
    C --> C2[排序选择]
    C --> C3[类别筛选]
    C --> C4[状态筛选]
    
    C1 --> C5[实时过滤列表]
    C2 --> C5
    C3 --> C5
    C4 --> C5
    
    D --> D1[点击资产卡片]
    D1 --> D2[查看资产详情]
    D2 --> D3{操作选择}
    D3 --> D4[编辑资产]
    D3 --> D5[删除资产]
    D3 --> D6[返回列表]
    
    E --> E1[点击+按钮]
    E1 --> F[进入添加页面]
    E --> E2[点击个人中心]
    E2 --> G[进入个人中心]
```

### 2.3 添加/编辑物品流程

```mermaid
graph TD
    A[进入添加页面] --> B[填写基础信息]
    B --> B1[输入商品名称*]
    B --> B2[选择商品分类*]
    B --> B3[选择商品图标*]
    B --> B4[输入购买价格*]
    B --> B5[选择购买时间]
    
    B --> C{展开高级设置?}
    C -->|是| D[填写高级信息]
    C -->|否| E[跳过高级设置]
    
    D --> D1[设置保修期限]
    D --> D2[设置预期使用年限]
    D --> D3[添加备注]
    D --> D4[添加自定义标签]
    
    D --> E
    E --> F[点击保存按钮]
    
    F --> G{验证必填项}
    G -->|未通过| H[显示错误提示]
    H --> B
    G -->|通过| I[保存数据]
    
    I --> J[计算初始折旧]
    J --> K[更新资产总览]
    K --> L[返回首页]
    L --> M[显示新增资产]
```

### 2.4 个人中心流程

```mermaid
graph TD
    A[个人中心] --> B[用户信息卡片]
    A --> C[特权功能区]
    A --> D[功能入口区]
    A --> E[其他选项区]
    
    B --> B1[显示头像昵称]
    B --> B2[显示用户ID]
    B --> B3[显示使用统计]
    B2 --> B4[点击复制ID]
    
    C --> C1[专属皮肤-锁定]
    C --> C2[自定义图标-锁定]
    C --> C3[更多特权-锁定]
    
    D --> D1[分类管理]
    D --> D2[年度总结]
    D --> D3[数据备份]
    D --> D4[数据恢复]
    
    D1 --> D1A[查看/编辑分类]
    D2 --> D2A[生成年度报告]
    D3 --> D3A[导出数据流程]
    D4 --> D4A[导入数据流程]
    
    E --> E1[常见问题]
    E --> E2[关注作者]
    E --> E3[看个广告]
    E --> E4[注销账号]
    
    E1 --> E1A[显示FAQ页面]
    E4 --> E4A[确认注销流程]
```

---

## 3. 详细功能流程

### 3.1 数据备份流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant UI as 界面
    participant L as 本地存储
    participant C as 云端(可选)
    
    U->>UI: 点击数据备份
    UI->>L: 读取所有资产数据
    L->>UI: 返回数据
    UI->>UI: 生成备份文件(JSON)
    UI->>U: 显示备份选项
    
    alt 本地备份
        U->>UI: 选择本地保存
        UI->>L: 保存到下载目录
        L->>UI: 保存成功
        UI->>U: 提示备份完成
    else 云端备份
        U->>UI: 选择云端保存
        UI->>C: 上传数据
        C->>UI: 上传成功
        UI->>U: 提示备份完成
    end
```

### 3.2 数据恢复流程

```mermaid
sequenceDiagram
    participant U as 用户
    participant UI as 界面
    participant L as 本地存储
    participant V as 数据验证
    
    U->>UI: 点击数据恢复
    UI->>U: 显示警告(将覆盖现有数据)
    U->>UI: 确认恢复
    UI->>U: 选择备份文件
    U->>UI: 选择文件
    UI->>V: 验证文件格式
    
    alt 格式正确
        V->>UI: 验证通过
        UI->>L: 备份当前数据
        L->>UI: 备份完成
        UI->>L: 导入新数据
        L->>UI: 导入成功
        UI->>U: 提示恢复完成
        UI->>UI: 刷新界面
    else 格式错误
        V->>UI: 验证失败
        UI->>U: 提示文件错误
    end
```

### 3.3 资产数据更新流程

```mermaid
sequenceDiagram
    participant S as 系统定时器
    participant C as 计算引擎
    participant D as 数据库
    participant UI as 界面
    
    Note over S,UI: 每日零点或应用启动时
    S->>C: 触发更新
    C->>D: 获取所有资产
    D->>C: 返回资产列表
    
    loop 每个资产
        C->>C: 计算使用天数
        C->>C: 计算每日成本
        C->>C: 计算当前残值
        C->>D: 更新资产数据
    end
    
    C->>C: 计算总残值
    C->>C: 计算今日成本
    C->>D: 更新统计数据
    D->>C: 保存成功
    C->>UI: 通知更新完成
    UI->>UI: 刷新显示
```

---

## 4. 用户操作路径

### 4.1 快速添加物品（最短路径）

```
首页 → 点击+ → 输入名称 → 选择分类 → 选择图标 → 输入价格 → 保存
```
**预计时间：** 30-60秒

### 4.2 查看资产成本

```
首页 → 查看资产列表 → 查看每日成本
```
**预计时间：** 5秒

### 4.3 筛选特定类别资产

```
首页 → 点击"全部类别" → 选择类别 → 查看筛选结果
```
**预计时间：** 10秒

### 4.4 数据备份完整流程

```
首页 → 个人中心 → 数据备份 → 选择备份方式 → 确认 → 完成
```
**预计时间：** 20-30秒

---

## 5. 页面状态管理

### 5.1 首页状态

| 状态 | 触发条件 | 显示内容 |
|------|---------|---------|
| 空状态 | 无任何资产 | 引导添加第一件物品 |
| 加载中 | 启动应用时 | 加载动画 |
| 正常状态 | 有资产数据 | 完整资产列表 |
| 筛选无结果 | 筛选条件无匹配 | "暂无匹配资产" |
| 错误状态 | 数据加载失败 | 错误提示+重试按钮 |

### 5.2 添加页面状态

| 状态 | 触发条件 | 显示内容 |
|------|---------|---------|
| 初始状态 | 进入页面 | 空表单 |
| 填写中 | 用户输入 | 实时验证 |
| 验证错误 | 必填项缺失 | 红色提示文字 |
| 保存中 | 点击保存 | 按钮loading |
| 保存成功 | 保存完成 | 返回首页 |

### 5.3 个人中心状态

| 状态 | 触发条件 | 显示内容 |
|------|---------|---------|
| 正常显示 | 进入页面 | 完整信息 |
| 处理中 | 执行操作 | loading状态 |
| 功能锁定 | 未解锁功能 | 锁定图标+提示 |

---

## 6. 数据流向图

```mermaid
graph LR
    A[用户输入] --> B[表单验证]
    B --> C[本地存储]
    C --> D[数据计算]
    D --> E[更新UI]
    
    C --> F[定时任务]
    F --> D
    
    G[应用启动] --> C
    C --> D
    
    H[数据备份] --> I[文件系统]
    I --> J[云端可选]
    
    K[数据恢复] --> C
    
    style C fill:#6366F1
    style D fill:#8B5CF6
    style E fill:#EC4899
```

---

## 7. 错误处理流程

### 7.1 常见错误场景

```mermaid
graph TD
    A[错误发生] --> B{错误类型}
    
    B -->|数据验证失败| C[表单字段提示]
    B -->|数据保存失败| D[重试机制]
    B -->|数据加载失败| E[显示错误页面]
    B -->|网络错误| F[离线提示]
    
    C --> G[用户修正]
    D --> H{重试次数}
    H -->|<3次| I[自动重试]
    H -->|>=3次| J[提示用户]
    
    E --> K[提供重试按钮]
    F --> L[切换离线模式]
    
    G --> M[继续操作]
    I --> M
    J --> N[用户决策]
    K --> M
    L --> M
```

---

## 8. 性能优化考虑

### 8.1 列表加载优化

- **虚拟滚动：** 资产超过50个启用虚拟列表
- **分页加载：** 每页20条数据
- **图片懒加载：** 滚动到可视区域才加载图标

### 8.2 计算优化

- **缓存机制：** 缓存计算结果，避免重复计算
- **异步计算：** 大量数据时使用Web Worker
- **增量更新：** 只更新变化的数据

### 8.3 存储优化

- **数据压缩：** 备份文件使用压缩
- **定期清理：** 清理过期缓存
- **索引优化：** 为常用查询字段建立索引

---

## 9. 页面跳转映射表

| 起始页面 | 操作 | 目标页面 | 返回路径 |
|---------|------|---------|---------|
| 首页 | 点击+ | 添加页面 | 返回按钮/保存后 |
| 首页 | 点击个人中心 | 个人中心 | 返回按钮 |
| 首页 | 点击资产卡片 | 资产详情 | 返回按钮 |
| 添加页面 | 点击保存 | 首页 | 自动返回 |
| 个人中心 | 分类管理 | 分类管理页 | 返回按钮 |
| 个人中心 | 年度总结 | 年度报告页 | 返回按钮 |
| 个人中心 | 数据备份 | 备份确认弹窗 | 取消/完成 |
| 个人中心 | 常见问题 | FAQ页面 | 返回按钮 |

---

**文档版本：** v1.0  
**最后更新：** 2026-03-06  
**文档所有者：** S.S.
