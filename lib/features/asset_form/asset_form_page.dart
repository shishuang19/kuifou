import 'package:flutter/material.dart';

class AssetFormPage extends StatelessWidget {
  const AssetFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('添加物品')),
      body: const Center(
        child: Text('资产表单页面开发中'),
      ),
    );
  }
}
